<nav class="navbar navbar-expand-lg" data-turbo-permanent>
  <div class="container-fluid">
    <%= link_to 'ALPHA BLOG', root_path, class: 'navbar-brand', id: 'logo' %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <!--Navigation Options-->
    <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
      <div class="d-flex align-items-center gap-3">
        <ul class="navbar-nav ms-auto me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <%= link_to 'Bloggers', users_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to 'About', about_path, class: 'nav-link' %>
          </li>
          <li class="nav-item dropdown">
            <a class="dropdown-toggle nav-link" href="#" id="articlesDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false" data-turbo="false">
              Articles
            </a>
            <ul class="dropdown-menu" aria-labelledby="articlesDropdown" data-bs-auto-close="false">
              <li><%= link_to 'All Articles', articles_path, class: 'dropdown-item', data: { turbo: false } %></li>
              <% if logged_in? %>
                <li><%= link_to 'Create Article', new_article_path, class: 'dropdown-item', data: { turbo: false } %></li>
              <% end %>
            </ul>
          </li>
          <% if logged_in? %>
            <li class="nav-item dropdown">
              <a class="dropdown-toggle nav-link" href="#" id="profileDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false" data-turbo="false">
                <%= "(Admin)" if current_user.admin? %> Profile <%= current_user.username %>
              </a>
              <ul class="dropdown-menu" aria-labelledby="profileDropdown" data-bs-auto-close="false">
                <li><%= link_to 'View Profile', user_path(current_user), class: 'dropdown-item', data: { turbo: false } %></li>
                <li><%= link_to 'Edit Profile', edit_user_path(current_user), class: 'dropdown-item', data: { turbo: false } %></li>
                <li><%= link_to 'Delete Profile', user_path(current_user), class: 'dropdown-item text-danger', data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } %></li>
              </ul>
            </li>
            <li class="nav-item">
              <%= link_to 'Logout', login_path, class: 'nav-link', data: { turbo_method: :delete } %>
            </li>
          <% else %>
            <li class="nav-item">
              <%= link_to 'Log in', login_path, class: 'nav-link' %>
            </li>
            <li class="nav-item">
              <%= link_to 'Sign up', new_user_path, class: 'nav-link' %>
            </li>
          <% end %>
        </ul>
        <form id="search-form" class="d-flex" role="search">
          <input id="search-input" class="form-control me-2" type="search" placeholder="Search" aria-label="Search"/>
          <button class="btn" id="btn-custom" type="submit">Search</button>
        </form>
      </div>
    </div>
  </div>
</nav>